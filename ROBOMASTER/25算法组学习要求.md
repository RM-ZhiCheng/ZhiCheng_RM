---
标题: 25算法组学习要求
修改日期: 2026-09-13
状态: 修改中
编写人: 郑邦合
---

# 25 算法组学习要求

## 一、算法组成员要求

### 1.1 本组必备技能

**环境** —— 分清 Linux 与 Windows 的区别，熟练使用命令行和终端；会装虚拟机、WSL、双系统；在 Linux 里装好 ROS 2；会用 VS Code。

**算法** —— 了解 cv2 与 YOLO；掌握完整的训练链条：采数据 → 标注 → 训练 → 导出 ONNX → 转 OpenVINO IR → 部署验证。

**接口与硬件** —— 懂通信协议（串口 / CAN、数据格式、校验方式）；会相机内参标定与手眼标定；了解镜头选型、曝光设置、雷达的安装角度与坐标系。

**实时性** —— 自瞄和导航是实时系统，不能只看"能不能跑"，还要看帧率和延迟。参照指标：自瞄帧率约 100 FPS。

**协作** —— 会用 Git（分支、提交、PR）；会用 AI 处理技术难点和重复性工作；会用 Obsidian 维护知识库；主动把学过的东西写成文档。

**开源代码** —— 读懂同济 SuperPower（自瞄）和北极熊（导航）的代码，理解模块划分，能按战队需求修改结构和参数。

> 优先级：环境 → 自瞄主流程 → 导航主流程 → 训练链 → 接口与硬件 → 协作工具。25 级以速成为主，时间不够时按这个顺序取舍。

### 1.2 算法组外但应当具备（面向 26 级）

本条主要面向 26 级。25 级以 1.1 为第一要务，以速成为主。

作为 RM 成员还应会：使用 AI（配置 API、掌握 harness 技巧）、焊接电路板与导线、使用单片机、C/C++/Python 基础（至少到函数）、拓竹 3D 打印与 Bambu Studio（上料 / 退料 / 检修）、基础 SolidWorks、翻墙（可选）。

## 二、学习路径

25 级面临的问题很直接：算法底层原理复杂，C++ 至少要学到面向对象，而 25 电气系的 C 语言课连指针和函数都没认真教。必须承认 25 级在代码层面约等于零基础，因此要在速成与打牢基础之间取平衡，且偏向速成。

起点是本部教材：上海大学【27SRM培训】算法组 1-5 讲。它默认你已学完 C++ 语言类内容，看不懂很正常，先整体概览一遍。

### 2.1 自瞄（先看这条）

1. 读 `readme.md`，重点看第 3、4 章（技术原理）
2. `src/standard.cpp`（83 行），把主流程看明白
3. `tests/auto_aim_test.cpp` —— 最有用的入口：不用相机、不用机器人，直接读 `assets/demo/demo.avi` 和配套的四元数文件就能跑。想改代码看效果就改它
4. 按需深入 `tasks/auto_aim/`，先看 `.hpp` 接口再看 `.cpp` 实现

### 2.2 导航

1. 读 `README.md`，重点是数据流和坐标变换。坐标系很多（`lidar_odom`、`odom`、`chassis`），看不懂这个后面全是懵的
2. 找入口：`pb2025_nav_bringup/launch/` 下的 `rm_navigation_reality_launch.py`（实车）和 `rm_navigation_simulation_launch.py`（仿真）
3. 看参数：`config/reality/nav2_params.yaml`，这是理解规划与控制最快的入口
4. 按包深入：先读 `package.xml` 的 description 和头文件，再看 `src/`

深入顺序：`point_lio`（依据 LOAM 论文）→ `small_gicp_relocalization` → `terrain_analysis` → `pb_nav2_plugins` → `pb_omni_pid_pursuit_controller`。数据转换桥遇到再看：`loam_interface`、`sensor_scan_generation`、`pointcloud_to_laserscan`、`fake_vel_transform`。

读代码前先补 Nav2 概念：建图、定位、代价地图（全局 / 局部）、全局规划器、局部控制器、行为树。

### 2.3 实践与仿真

虚拟机里没有工业相机、也没有可用显卡，仿真（`pb_rm_simulation`）是唯一能动手跑起来的环境。先把仿真跑通，再谈上真车。

### 2.4 环境清单（版本是绑定的）

Ubuntu 22.04 + ROS 2 Humble + OpenVINO 2024.6 + CMake 3.22 + OpenCV 4.5.4。

两个已踩过的坑：

- **换行符**：Windows 上 `core.autocrlf=true` 会把 LF 换成 CRLF，搬到 Linux 后脚本报 `bad interpreter`。解决：`git config --global core.autocrlf input`，或用 VS Code 远程连虚拟机编辑。
- **OpenVINO ABI**：Python 版 OpenVINO 包（含清华镜像）用的是旧 C++ ABI，和系统的 OpenCV、yaml-cpp 冲突，链接时报大量 `undefined reference`。要用 Intel 官方 apt 源的 `libopenvino-*` 包。判断方法：`nm -D --defined-only libopenvino.so | c++filt | grep read_model`，出现 `std::__cxx11::` 才是新 ABI。

### 2.5 资料链接

**自瞄**

- 同济大学 SuperPower 战队 25 赛季自瞄开源：https://github.com/TongjiSuperPower/sp_vision_25
- 往届版本：[sp_vision_23](https://github.com/TongjiSuperPower/sp_vision_23)、[sp_vision_24](https://github.com/TongjiSuperPower/sp_vision_24)

**导航**

- 北理莫斯科大学北极熊战队 25 赛季哨兵导航：https://github.com/SMBU-PolarBear-Robotics-Team/pb2025_sentry_nav
- 配套仿真环境：https://github.com/LihanChen2004/pb_rm_simulation
- Nav2 官方仓库：https://github.com/ros-navigation/navigation2

**依赖与文档**

- OpenVINO 官方文档：https://docs.openvino.ai/2024/index.html
- OpenVINO 的 Intel apt 源：https://apt.repos.intel.com/openvino/2024 （套件名是 `ubuntu22`）
- 相机 SDK：编译不需要，仓库自带头文件和库；配真实相机时再装海康 MVS 或迈德威视 SDK

**论文与教材**

- LOAM: Lidar Odometry and Mapping in Real-time（Zhang & Singh, RSS 2014）：http://www.roboticsproceedings.org/rss10/p07.pdf —— 导航里 `point_lio` 的前身
- 上海大学【27SRM培训】算法组 1-5 讲（本部提供，链接待补）

## 三、验收标准

- **最低**：掌握基本 C++，看得懂代码，知道 YOLO 和 cv2
- **合格**：掌握自瞄或导航的整体代码结构，会训练 YOLO，会用 cv2，能说清不同算法的优缺点与取舍，能做有限修改与优化，理解底层逻辑
- **优秀**：能完全理解算法原理，能以开源代码为基础搭出战队自己的自瞄 / 导航代码，能把 YOLO 与 cv 库结合使用，能针对性地改进算法

## 四、分工

自瞄算法少但调参多，导航算法多但调参少，两条线节奏不同，**在打基础之前就分好工**。项目管理、协调沟通、技术管理的问题直接问本部的人。

---

相关笔记：[[算法组开源代码学习路径]]